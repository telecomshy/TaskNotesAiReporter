# 「按标题」搜索的匹配语义

搜索框支持 `关键字`、`#标签`、`@上下文` 三种条件。决定：三个维度之间为 AND（都需满足），同一维度内多个条件为 OR；标签按层级前缀匹配（`#work` 命中 `work` 与 `work/子级`），上下文按精确匹配（`@work` 只命中 `work`，不命中 `workplace`——子串匹配会误命中），关键字只匹配任务标题。

反直觉之处是「多个关键字为 OR 而非 AND」——这是刻意选择（宽召回）；该语义曾被视为 bug 险些被改成 AND，记录在此以避免将来被误“修”。
