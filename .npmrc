# 本机 User 级设有 NODE_ENV=production，npm 据此默认 omit=dev；
# 而本仓库依赖全在 devDependencies，缺了它们 `npm install` 会静默变成空操作
# （随后 `prepare` 里的 husky 报“找不到命令”，误导排查方向）。
# include 优先于 omit，显式把 dev 依赖装回来，隔离这个环境坑。
include=dev
