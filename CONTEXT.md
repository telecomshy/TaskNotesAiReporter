# TaskNotes AI Reporter

一个服务于 Obsidian 的辅助插件：从 TaskNotes 读取任务，调用兼容 OpenAI 的模型，按日期区间与所选模板生成报告（周报 / 月报 / 年报或自定义）并写入新笔记。它的核心动作是「把一个任务集合塑造成一份报告」。

## Language

**任务（Task）**:
一个由 TaskNotes 插件管理、以 Markdown 笔记形式存储的工作项，带标题、状态、优先级，以及标签、上下文、项目等属性。
_Avoid_: 事项、条目、待办

**选择任务（Task Picker）**:
在主窗口「生成任务报告」流程中，用户以「按时间」或「按标题」两种方式筛选并挑取任务的交互窗口。它只呈现尚未「已加入」的任务；已加入的任务在主窗口中管理。

**已加入（Candidate）**:
进入主窗口报告列表、将纳入报告的任务。主列表即报告任务集合；从列表移除即不再纳入。
_Avoid_: 已选择、已勾选、选中

**可加入（Addable）**:
尚未「已加入」、因此在「选择任务」窗口中可被挑取的任务；是「已加入」的补集。
_Avoid_: 候选（「候选」易与 Candidate＝「已加入」混淆）、可选

**标签（Tag）**:
任务的可选分类标记，在存储中是不带前缀的字符串；在输入时用 `#` 前缀表示（如 `#前端`）。
_Avoid_: 分类、分组

**上下文（Context）**:
任务的可选使用场景标记，在存储中不带前缀；在输入时用 `@` 前缀表示（如 `@工作`）。
_Avoid_: 环境、场景

**关键字（Keyword）**:
「按标题」筛选里不带 `#`/`@` 前缀的裸词，仅用来匹配任务的标题。

**报告（Report）**:
插件针对某时间区间生成、写入 vault 新笔记的成果物，正文由模型据任务数据生成。
_Avoid_: 文档、总结

**报告类型（Report Type）**:
报告的周期口径，取值为 周报 / 月报 / 年报 / 自定义。

**界面语言（UI Language）**:
插件界面文案所用的语言，取值为 自动 / 中文 / 英文；「自动」时跟随 Obsidian 的显示语言（`zh`/`zh-TW` → 中文，其余 → 英文）。只影响界面外壳，不影响报告内容。
_Avoid_: 语言、locale、语言设置

**报告语言（Report Language）**:
期望生成的报告正文所用的语言。只约束报告产出，不约束喂给模型的任务数据表示。与「界面语言」相互独立：界面可英文而报告仍为中文，反之亦然。
_Avoid_: 语言、输出语言

**报告模板（Template）**:
一份可复用的报告生成指令，含标题与内容；内容里用占位符引用任务、时间范围等信息。用户可选择某个模板来约束生成，或不选模板（极简模式）。
_Avoid_: 提示词、格式

**日期范围（Date Range）**:
报告所覆盖的时间区间（闭区间，YYYY-MM-DD）。由所选任务的日期推导，与「选择任务」时选定的筛选区间无关。
_Avoid_: 报告周期、报告区间

**筛选区间（Filter Range）**:
「选择任务」窗口「按时间」里用于过滤候选任务的区间；只决定挑哪些任务，不决定「日期范围」。
_Avoid_: 日期范围、报告范围

**日期口径（Date Field）**:
自动筛选中参与的日期字段（完成时间、到期时间、计划时间、创建时间）。任务在所选区间命中任一选定字段即纳入。

**自动筛选（Auto-filter）**:
依据「日期口径」把落在所选「日期范围」内的任务自动纳入候选的规则；未命中任何口径的任务需由用户手动添加。

**供应商（Provider）**:
提供模型接口的 OpenAI 兼容服务（如 DeepSeek、通义千问、Kimi），分内置预设与自定义两类。

**密钥（Secret）**:
供应商认证所用的凭据值，保存在 Obsidian 的 SecretStorage 中；插件设置只保存它的「密钥名」，生成报告或拉取模型时才按名取值。
_Avoid_: API Key、令牌、凭据

**密钥名（Secret id）**:
密钥在 Obsidian SecretStorage 中的标识。插件设置只保存它；重命名与删除归 Obsidian 的「密钥存储」管理，插件不擅自改名（该名可能被其它插件共享引用）。
_Avoid_: 密钥 ID、key 名、密钥名称

**模型（Model）**:
具体用于生成报告的大语言模型，隶属于某个供应商，可携带上下文长度与最大输出限制。

**当前模型（Active Model）**:
用户从已配置供应商的模型中选择、用于生成报告的那一个。报告生成据此调用模型；其凭据取自该供应商设置里的「密钥名」，运行时按名向 SecretStorage 取值。

